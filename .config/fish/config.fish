if status is-interactive
    # Commands to run in interactive sessions can go here
end

bind \cy forward-word
bind \ey forward-char

alias vi=nvim

set PATH /home/philip/.nvm/versions/node/v16.15.0/bin $PATH
